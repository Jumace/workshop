# Optional: put this in .env.development.local for local development.
# Production defaults to https://cebulla.dev when this is unset.
SITE_URL=http://localhost:3000

# Optional: local fallback for the Vercel Flags lab accent.
LAB_FLAGS_FALLBACK=cyan
